opus_audit:
    # No global auto-purge default; entities opt in via #[Retention(...)].
    retention:
        default: null

    # The crypto-shredding key is derived from the Symfony application secret
    # (APP_SECRET) by default — nothing else to configure. Override the
    # SubjectKeyProviderInterface service to use stored per-subject keys instead.
